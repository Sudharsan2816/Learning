# AI/ML Learning Platform

Interactive React/TypeScript learning app for AI, machine learning, LLM concepts, and public API practice.

This repository was previously named `Learning`. The recruiter-friendly name should be `ai-ml-learning-platform` because the project is a developer-advocacy/product build, not a generic learning folder.

## Project Location

The app source lives in:

```text
ai-ml-learning-platform/
```

Legacy standalone HTML/PDF learning assets are also present at the repository root. They should be cleaned up or moved into `docs/` before this repo is pinned.

## Portfolio Role

This is not the strongest repo for Data Engineer or ML Engineer roles, but it is useful for Developer Advocate, AI educator, and product-building signal. Keep it public, improve the README and repo name, but pin it only after the core AI/backend repos are polished.

Recommended description:

`Interactive AI/ML learning platform with lesson progress tracking, concept search, and public API playground built with React, TypeScript, and Vite.`

Recommended topics:

`react`, `typescript`, `vite`, `ai-education`, `machine-learning`, `llm`, `public-apis`, `developer-education`
